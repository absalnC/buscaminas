describe("getPressAction"){
	it("expects a number for row, else throw",()=>{});
	it("expects an integer for row, else throw",()=>{});
	it("expects positive for row,else throw",()=>{})
	it("expects a number for col, else throw",()=>{});
	it("expects an integer for col, else throw",()=>{});
	it("expects positive for col,else throw",()=>{})
}